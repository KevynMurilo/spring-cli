# Activate test profile
spring.profiles.active=test

# Disable Spring Boot banner during tests
spring.main.banner-mode=off

# Reduce logging during tests
logging.level.root=WARN
logging.level.com.springcli=INFO
